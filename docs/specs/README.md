# docs/specs

Spec registry for scoped implementation work.

What belongs here:
- numbered spec directories (`NNN-<name>`)
- each spec's `spec.md`, tasks, architecture, QA, risks, and implementation notes
- no implementation code
