# packages/types

Canonical shared TypeScript types and interfaces.

What belongs here:
- domain types and API contract types
- cross-package shared schemas and constants
- no runtime business side effects
