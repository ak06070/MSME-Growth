# packages/connectors

Connector framework for ingestion pipelines (CSV first, extensible later).

What belongs here:
- connector lifecycle interfaces
- ingestion normalization and validation helpers
- connector-specific adapters as approved by specs
