# apps

Contains deployable applications for the MSME platform.

What belongs here:
- runtime app projects (web, api)
- app-level configuration and startup scripts
- no shared cross-app libraries (use `packages/*`)
